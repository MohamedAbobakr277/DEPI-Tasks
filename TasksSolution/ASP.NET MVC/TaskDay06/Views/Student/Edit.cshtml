@model EFCoreTask.ViewModels.StudentFormViewModel
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Edit Student - @Model.Student.Name</title>
    <style>
        body { font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; margin: 40px; background-color: #f8f9fa; color: #343a40; }
        .form-container { background-color: #fff; padding: 35px; border-radius: 8px; box-shadow: 0 4px 15px rgba(0,0,0,0.1); max-width: 500px; margin: 0 auto; border-top: 5px solid #ffc107; }
        h1 { margin-top: 0; color: #ffc107; text-align: center; border-bottom: 2px solid #ffc107; padding-bottom: 10px; margin-bottom: 25px; }
        .form-group { margin-bottom: 20px; }
        label { display: block; margin-bottom: 8px; font-weight: bold; color: #495057; }
        input[type="text"], input[type="number"], select { width: 100%; padding: 12px; border: 1px solid #ced4da; border-radius: 5px; box-sizing: border-box; font-size: 16px; background-color: #fff;}
        input:focus, select:focus { border-color: #ffc107; outline: none; box-shadow: 0 0 5px rgba(255, 193, 7, 0.5); }
        .btn-submit { display: block; width: 100%; padding: 14px; background-color: #ffc107; color: black; border: none; border-radius: 5px; font-size: 16px; font-weight: bold; cursor: pointer; transition: opacity 0.2s; margin-top:30px;}
        .btn-submit:hover { opacity: 0.8; }
        .back-link { display: block; text-align: center; margin-top: 20px; color: #6c757d; text-decoration: none; font-weight:bold; }
        .back-link:hover { text-decoration: underline; color: #343a40; }
    </style>
</head>
<body>

    <div class="form-container">
        <h1>Amend Student File</h1>

        <form asp-action="Edit" asp-controller="Student" method="post">
            @Html.AntiForgeryToken()
            <input type="hidden" asp-for="Student.Id" />

            <div class="form-group">
                <label asp-for="Student.Name">Full Legal Name:</label>
                <input asp-for="Student.Name" type="text" required placeholder="Ex. John Doe" />
            </div>

            <div class="form-group">
                <label asp-for="Student.Age">Age:</label>
                <input asp-for="Student.Age" type="number" required min="10" max="100" />
            </div>

            <div class="form-group">
                <label asp-for="Student.DepartmentId">Assign Department:</label>
                <select asp-for="Student.DepartmentId" asp-items="Model.Departments" required>
                    <option value="">-- Choose Target Department --</option>
                </select>
            </div>

            <button type="submit" class="btn-submit">Upload Changes</button>
        </form>

        <a asp-action="GetAll" class="back-link">Cancel</a>
    </div>

</body>
</html>
